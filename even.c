#include<stdio.h>
void main()
{
 int number;
 printf("Enter the number:");
 scanf("%d",&number);
 if(num % 2==0)
 {
 printf("%d is even.", number);
 }
 else {
 printf("%d is odd.", number);
 }
 return 0;
 }
